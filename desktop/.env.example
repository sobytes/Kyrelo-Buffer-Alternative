# Required: Anthropic key for the @grok reply generator (default provider).
ANTHROPIC_API_KEY=

# Optional: OpenAI key — switch provider to OpenAI in Settings to use it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
